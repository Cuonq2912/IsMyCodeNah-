#include<bits/stdc++.h>
using namespace std;

using ll = long long;
using dungsai = bool;
using nguyen = int;

typedef long long ll;

#define ll long long
#define kitu char

#define pi 3.14
#define max 1000000000

#define xinchao cout << "Ntc\n";
#define timmax(a,b) cout << (a < b ? a : b);
#define FastIO ios_base::sync_with_stdio(0); cin.tie(0);
#define FOR(i, a, b) for(int i = a; i <= b; i++)
#define FORd(i, a, b) for(int i = a; i >= b; i--)

int main(){
    FastIO;
    FOR(i, 10, 20){
        cout << i << " ";
    }
    FORd(i, 20 10){
        cout << i << " ";
    }
    return 0;
}